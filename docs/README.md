# tono docs

The documentation lives at **https://marmikshah.github.io/tono/** — the sources are this directory.

Build locally from the repo root: `npm run docs:dev` (dev server) or `npm run docs:build` (static build into `docs/.vitepress/dist`).
